using Godot;
using System;

public partial class Sketch: Node3D
{
	// static Rectangle boundary = new Rectangle(0,0,5,5);
	// [Export] public QuadTree qt = new QuadTree(boundary,4);
	//
	// public override void _Ready(){
	// 	for (int i = 0; i < 20; i++)
	// 	{
	// 	    Point p = new Point(GD.RandRange(-boundary.GetW(),boundary.GetW()),GD.RandRange(-boundary.GetH(),boundary.GetH()));
	// 		GD.Print("point : " + p + "\n added to quadtree with position.\n x : " + p.GetX() + "\n y : " + p.GetY());
	// 		qt.Insert(p);
	// 	}
	// }
}
